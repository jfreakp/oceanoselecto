version: '3.8'


services:
  db:
    image: postgres:16 
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: db_password_123
      POSTGRES_DB: db_oceanoselecto
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
